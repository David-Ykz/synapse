models:
  model1:
    name: "example_llm_model_name"
    temperature: 0.5
tools:
  fetch_hotels:
   z endpoint: "./fetch_hotels"
  fetch_flights:
    endpoint: "./fetch_flights
agents:
  hotel_agent:
    input_namespace: hotels
    output_namespace: completed
    model: model1
    prompt: "./hotels_agent.json"
    tools:
      - fetch_hotels
  flight_agent:
    namespace: flights
    prompt: "./flight_agent.json"
    tools:
      - fetch_flights

# input | namespace | completed | output